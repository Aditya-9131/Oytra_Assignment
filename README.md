# Oytra_Assignment
Built a Python script to clean messy signup data by standardizing dates, removing duplicates, separating low-quality leads into a quarantine file, and handling multi-plan users while keeping the most recent record. The output is a clean, CRM-ready “Golden Record” CSV.
